import { AppSettings, QualityType } from '../types';

const Settings: AppSettings = {
  dpr: 1,
  fps: 30,
  width: 100,
  height: 100,
  mobile: false,
  supportOffScreenCanvas: false,
  quality: QualityType.Low,
}
export default Settings
